package com.ziro.bullet.interfaces;

public interface LanguageInterface {
    void languageResult(int size);

    void loaderShow(boolean flag);

    void error(String error);

    void success(Object response);
}
